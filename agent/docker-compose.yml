services:
  agent:
    build: .
    environment:
      PG_DSN: postgresql://pgai_agent:CHANGE_ME@127.0.0.1:5432/postgres
      OPENAI_API_KEY: CHANGE_ME
      MODEL: gpt-4o-mini
      MAX_STATEMENTS: 50
      AGENT_PORT: 8010
    network_mode: host
